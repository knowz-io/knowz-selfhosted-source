{
  "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentTemplate.json#",
  "contentVersion": "1.0.0.0",
  "metadata": {
    "_generator": {
      "name": "bicep",
      "version": "0.39.26.7824",
      "templateHash": "350959936198991346"
    }
  },
  "parameters": {
    "principalId": {
      "type": "string",
      "metadata": {
        "description": "Principal ID (managed identity principalId) that receives the role assignment."
      }
    },
    "roleDefinitionId": {
      "type": "string",
      "metadata": {
        "description": "Role definition ID (GUID). Must be a subscription-scope reference, e.g. subscriptionResourceId(\"Microsoft.Authorization/roleDefinitions\", \"<guid>\")."
      }
    },
    "targetResourceId": {
      "type": "string",
      "metadata": {
        "description": "Target resource ID. Used only for deterministic name hashing — caller is responsible for scoping the module to the target RG."
      }
    },
    "principalType": {
      "type": "string",
      "defaultValue": "ServicePrincipal",
      "allowedValues": [
        "ServicePrincipal",
        "User",
        "Group"
      ],
      "metadata": {
        "description": "Principal type. Default ServicePrincipal — set to User only for deployer pre-grant scenarios."
      }
    }
  },
  "resources": [
    {
      "type": "Microsoft.Authorization/roleAssignments",
      "apiVersion": "2022-04-01",
      "name": "[guid(parameters('targetResourceId'), parameters('principalId'), parameters('roleDefinitionId'))]",
      "properties": {
        "principalId": "[parameters('principalId')]",
        "roleDefinitionId": "[parameters('roleDefinitionId')]",
        "principalType": "[parameters('principalType')]"
      }
    }
  ],
  "outputs": {
    "roleAssignmentId": {
      "type": "string",
      "value": "[resourceId('Microsoft.Authorization/roleAssignments', guid(parameters('targetResourceId'), parameters('principalId'), parameters('roleDefinitionId')))]"
    }
  }
}