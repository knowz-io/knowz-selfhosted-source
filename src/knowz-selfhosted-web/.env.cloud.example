# Copy this to .env.cloud (gitignored) to enable npm run dev:cloud.
# The default stays on your local API. Replace it with an HTTPS endpoint you
# administer before intentionally connecting this development client remotely.

VITE_PROXY_TARGET=http://localhost:5000
