# ===========================================================================
# Knowz Self-Hosted — Environment Configuration
# ===========================================================================
# Copy this file to .env and configure ONE AI tier below.
# .env is gitignored and will not be committed.
#
# EASIEST PATH — use the interactive Setup wizard instead of hand-editing:
#   dotnet run --project src/Knowz.SelfHosted.Setup
#
# Otherwise, AI TIERS (pick ONE):
#   Tier 4: No AI (default)           — CRUD, auth, file uploads. No search/Q&A.
#   Tier 1: Knowz Platform (easiest)  — Just paste your Knowz API key. All AI works.
#   Tier 2: OpenAI-compatible         — LM Studio, Ollama, vLLM, OpenAI, etc.
#   Tier 3: Direct Azure (advanced)   — Your own Azure OpenAI + AI Search resources.
#
# Tier naming matches docs/CONFIGURATION.md and the AppHost README.
#
# QUICK START (recommended — get AI with one key):
#   cp .env.example .env
#   # Set required secrets below, then uncomment the "TIER 1: KNOWZ PLATFORM"
#   # section and paste your API key.
#   docker compose up --build
# ===========================================================================

# --- Core Settings (always active) -----------------------------------------
# SECURITY: No default values for secrets. docker compose will fail to start
# if any of these are left empty — this is intentional, to prevent accidental
# deployment with weak/shared credentials. Generate fresh values per deployment.
#
# Generate a strong random secret (Linux/macOS):
#   openssl rand -base64 48 | tr -d '/+=\n' | head -c 64   # JWT_SECRET
#   openssl rand -base64 24                                # ADMIN_PASSWORD / POSTGRES_PASSWORD
# Windows PowerShell:
#   [Convert]::ToBase64String((1..48 | %{Get-Random -Max 256}) | %{[byte]$_})

# Postgres password — REQUIRED
POSTGRES_PASSWORD=

# JWT secret for authentication tokens — REQUIRED (min 32 chars, cryptographically random)
JWT_SECRET=

# SuperAdmin credentials — REQUIRED, created on first startup.
# ADMIN_PASSWORD must be >=12 chars, pass complexity rules, and NOT appear on the
# weak-password denylist in AuthService.WeakPasswordList (admin/changeme/password/etc).
ADMIN_USERNAME=
ADMIN_PASSWORD=
# Set true only when ADMIN_PASSWORD was generated for a new first boot.
ADMIN_PASSWORD_REQUIRES_CHANGE=false

# MCP service-to-service key — REQUIRED (same value on both API and MCP containers)
MCP_SERVICE_KEY=

# ===========================================================================
# TIER 1: Knowz Platform (RECOMMENDED — easiest, one API key)
# ===========================================================================
# All AI features powered by your Knowz account. Just paste your API key.
# Get your key at https://knowz.io → Settings → API Keys.
# No Azure subscription needed. Search uses local embeddings.
# Uncomment ALL three lines below and paste your API key.
# ---------------------------------------------------------------------------
# KNOWZ_PLATFORM_ENABLED=true
# KNOWZ_PLATFORM_URL=https://api.knowz.io
# KNOWZ_PLATFORM_APIKEY=your-platform-api-key

# ===========================================================================
# TIER 2: OpenAI-compatible (local or hosted)
# ===========================================================================
# For a model server on this Docker host, use host.docker.internal rather than
# localhost. API key may stay blank for a keyless local server.
# ---------------------------------------------------------------------------
# OPENAI_COMPAT_ENDPOINT=http://host.docker.internal:1234/v1
# OPENAI_COMPAT_APIKEY=
# OPENAI_COMPAT_CHAT_MODEL=your-chat-model
# OPENAI_COMPAT_EMBEDDING_MODEL=your-embedding-model
# EMBEDDING_MODEL_NAME=your-embedding-model
# EMBEDDING_DIMENSIONS=768

# ===========================================================================
# TIER 3: Direct Azure (advanced — bring your own Azure resources)
# ===========================================================================
# Full-featured: vector search, embeddings, AI summarization, Q&A.
# Requires your own Azure OpenAI and Azure AI Search resources.
# Uncomment ALL lines below and fill in your endpoint + key values.
# ---------------------------------------------------------------------------
# AZURE_OPENAI_ENDPOINT=https://your-openai.openai.azure.com/
# AZURE_OPENAI_APIKEY=your-azure-openai-key
# AZURE_OPENAI_DEPLOYMENT=gpt-5.4
# AZURE_OPENAI_EMBEDDING=text-embedding-3-large
# Embedding model + dim MUST match your Azure deployment:
#   text-embedding-3-large -> 3072 (used here by default)
#   text-embedding-3-small / ada-002 -> 1536
# Override both if you deploy a different model.
# EMBEDDING_MODEL_NAME=text-embedding-3-large
# EMBEDDING_DIMENSIONS=3072
# AZURE_SEARCH_ENDPOINT=https://your-search.search.windows.net
# AZURE_SEARCH_APIKEY=your-azure-search-admin-key
# AZURE_SEARCH_INDEX=knowz-selfhosted

# --- Optional: Azure AI Vision (image/diagram analysis — caption, tags, objects, OCR)
# AZURE_AI_VISION_ENDPOINT=https://your-vision.cognitiveservices.azure.com/
# AZURE_AI_VISION_APIKEY=your-azure-vision-key

# --- Optional: Azure Document Intelligence (advanced PDF/Office extraction with layout)
# AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-docintel.cognitiveservices.azure.com/
# AZURE_DOCUMENT_INTELLIGENCE_APIKEY=your-docintel-key

# ===========================================================================
# OPTIONAL: MCP Server
# ===========================================================================
# The MCP (Model Context Protocol) server enables AI tools (Claude, etc.) to
# interact with your knowledge base. It proxies requests to the API.
# These defaults work out of the box — change only if needed.
# ---------------------------------------------------------------------------
# MCP_PORT=3001
# MCP_BACKEND_MODE=selfhosted
# MCP_VALIDATE_API_KEY=true
# MCP_API_URL=http://api:8080
# MCP_API_KEY_VALIDATION_ENDPOINT=/api/vaults

# ===========================================================================
# OPTIONAL: Storage
# ===========================================================================
# Default: local filesystem (files stored in Docker volume).
# Uncomment to use Azure Blob Storage instead.
# ---------------------------------------------------------------------------
# STORAGE_PROVIDER=AzureBlobStorage
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=your-account;...
# AZURE_STORAGE_CONTAINER=selfhosted-files

# ===========================================================================
# OPTIONAL: SSO (Single Sign-On)
# ===========================================================================
# SSO is configured via the Admin UI (Settings > SSO Configuration), not via
# environment variables. Supported providers: Microsoft Entra ID (Azure AD).

# ===========================================================================
# ADVANCED (defaults work for most deployments)
# ===========================================================================
# RATE_LIMITING_ENABLED=true
# ALLOWED_ORIGIN=http://localhost:3000
#
# Swagger/OpenAPI is off. Enable only for local debug:
# ENABLE_SWAGGER=true
#
# AllowedHosts default is localhost + IPv4/IPv6 loopback (not *).
# Required when the API/MCP are reached by a public or custom Host header:
# ALLOWED_HOSTS=knowz.example.com;localhost;127.0.0.1;api;mcp
#
# Database is internal-only (no host port). To reach Postgres from the host:
#   1. Uncomment the db ports block in docker-compose.yml (loopback bind).
#   2. Optionally set DB_PORT if 5432 is already taken on the host.
# DB_PORT=5432
