# =============================================================================
# NETWORK — Standard (no VNet)
# =============================================================================
# The standard self-hosted deployment uses public endpoints for all services.
# For enterprise deployments with VNet isolation, see the enterprise module.
